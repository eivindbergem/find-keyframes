#!/usr/bin/env python

from distutils.core import setup

setup(name='find-keyframes',
      version='0.1',
      description='Find keyframes in MPEG TS files.',
      author='Eivind Alexander Bergem',
      author_email='eivind.bergem@gmail.com',
      url='https://github.com/peivind/find-keyframes',
      packages=['findkeyframes'],
      )
